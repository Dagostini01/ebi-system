# URL base da API (sem barra no final). Deixe vazio para usar só o mock em memória.
# VITE_API_BASE_URL=https://api.exemplo.com

# Acesso master (secreto — não compartilhe; use rota /acesso-interno)
VITE_MASTER_EMAIL=seu-email@privado.com
VITE_MASTER_PASSWORD=sua-senha-secreta
